//! Constants used throughout the program for PDA seeds and configuration.

/// Seed for the global configuration PDA.
pub const CONFIG: &str = "config";

/// Seed for the global vault PDA.
pub const GLOBAL: &str = "global";

/// Seed for metadata PDA (reserved for future use).
pub const METADATA: &str = "metadata";

/// Number of decimals for SOL (lamports).
pub const LAMPORT_DECIMALS: u8 = 9;

/// Seed for token launch PDA (reserved for future use).
pub const TOKEN_LAUNCH: &str = "token_launch";

/// Seed for the game ground PDA.
pub const GAME_GROUND: &str = "BONDING_CURVE";
